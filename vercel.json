{
  "version": 2,
  "installCommand": "echo 'Python deps handled by runtime'",
  "buildCommand": "if command -v g++ >/dev/null; then g++ -O2 -std=c++17 game/engine/main.cpp -o game/engine/main && chmod +x game/engine/main; else echo 'g++ compiler not available, falling back to Python engine'; fi && mkdir -p public && echo '<!-- -->' > public/placeholder.html",
  "outputDirectory": "public",
  "functions": {
    "api/wsgi.py": {
      "includeFiles": "game/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/wsgi" }
  ]
}
